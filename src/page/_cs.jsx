import * as React from 'react';
import {MainTemplate} from '../components';
import {CsContainer} from '../containers';
        
const Cs = () =>(
    <MainTemplate>
        <CsContainer />
    </MainTemplate>
);

export default Cs;